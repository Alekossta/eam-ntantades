import { createTheme } from "@mui/material";

const theme = createTheme({
    palette:
    {
        primary:
        {
            main: "#D9D9D9"
        },
        secondary:
        {
            main: "#FFA771"
        }
    }
})

export default theme;