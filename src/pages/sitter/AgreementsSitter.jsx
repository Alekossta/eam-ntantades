import { Box } from "@mui/material";
import { collection, doc } from "firebase/firestore";
import { useEffect } from "react";

export default function AgreementsSitter()
{
    useEffect(() => {
        try
        {
           
        }
        catch(e)
        {
            console.log(e);
        }
    }, []);
    return <Box>
        hello              
    </Box>
}